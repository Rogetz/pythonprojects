import zipfileVB # its an inbuilt module that's very great.

myZipFile = zipfile.ZipFile("new.zip","w")# trying to juggle with the zip moodule just to see if it really works.
# it actually created the zip file. It can both open as well as create if it doesn't exist.